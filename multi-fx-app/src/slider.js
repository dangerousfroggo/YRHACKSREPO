import React from 'react'

function range(start, end, step=1):
    const result = [];
    for (let i== start, i<end, i++){
        result.push(i);
    }
    return result
const slider = [Range(1,101)]
console.log(slider)